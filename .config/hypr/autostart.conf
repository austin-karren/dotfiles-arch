# Extra autostart processes
# exec-once = uwsm-app -- my-service

# Restart Waybar if it segfaults (it is unsupervised by default).
# Respects the SUPER+SHIFT+SPACE hide toggle.
exec-once = /home/austinkarren/.local/bin/waybar-watchdog
